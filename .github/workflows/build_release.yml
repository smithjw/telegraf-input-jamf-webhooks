on:
  workflow_dispatch:
  # release:
  #   types: [created]

permissions:
    contents: write
    packages: write

jobs:
  build_and_release:
    name: Build and Release (${{ matrix.goos }}_${{ matrix.goarch }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux] # [linux, windows, darwin]
        goarch: [amd64] # ["386", amd64, arm64]
        exclude:
          - goarch: "386"
            goos: darwin
          - goarch: arm64
            goos: windows
    steps:
    - uses: actions/checkout@v4
    - uses: wangyoucao577/go-release-action@6ac7dba1f9e61850053324549cb6bc88e4b473d2 # v1.51
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        goos: ${{ matrix.goos }}
        goarch: ${{ matrix.goarch }}
        # goversion: "1.23.1"
        project_path: "cmd/main.go"
        binary_name: "jamf-webhooks"
        upload: false
        # extra_files: LICENSE README.md
